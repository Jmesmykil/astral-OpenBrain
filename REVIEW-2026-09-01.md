# Reviewer approval and follow-ups — 2026-09-01

PROVENANCE: the creator forwarded the reviewer's message on 2026-09-01 (~23:00 HST)
with the direction "check on the submission and finish any requests they might
want and document this including what was said in the email." The message is
quoted verbatim below; everything after it is what was done in response.

## What the reviewer wrote

> Approving this. Genuinely good work.
>
> I checked the numbers rather than taking them on trust. Recomputed escape
> velocity and surface gravity for all 11 bodies, unit factors are exact defined
> values, and the grade algebra matches your README. Also ran it live on an agent
> and it answers correctly. The comments explaining your reasoning are better
> than most.
>
> Two things worth a follow up:
>
> The ZoneInfo(tz) line can silence the whole ability. get_timezone() can return
> a non IANA value, a padded string, or throw, and since it runs before
> astral_answer() a bad timezone kills every answer including ones with no clock
> in them. It gets swallowed into the log so the user hears nothing. Wrapping it
> with a fallback to datetime.now() fixed it.
>
> The platform comments out from future import annotations on upload. Your 10
> PEP 604 annotations then get evaluated eagerly, so the file quietly hard
> requires Python 3.10+. Safer to drop the future import and use Optional[str].
>
> One suggestion: "mass of earth" and "diameter of mars" return nothing, but
> _SCI_BODY already holds mass and radius for every body. Probably the most
> common planet questions and they're free from data you already have.
>
> Would also be good to ship the hub/ source and tests, since only the generated
> main.py lands here.

## The creator's direction

Finish their requests, document this, then "look at accepting their suggestions
and commits", then prepare the newer, improved version: possibly add Slate, and
"we definitely need a ranking system for all commands so that we KNOW when the
compute on the device cant handle it."

## What was checked first

PR #361 is merged. The maintainer approved it on 2026-08-31 and merged it into `dev`
the same minute (merge `7a8aed8`). Before merging, the maintainer pushed two commits
onto the branch: every annotation changed to `Optional[...]`, and a `_now()` helper
that falls back to the local clock when `get_timezone()` returns junk or throws. So the
first two follow-ups were already applied upstream, and the workspace had to take
them back, not re-do them.

## Done in response

- The maintainer's `_now()` is in `community/astral-skill/main.py` word for word, and
  the `from __future__ import annotations` line is gone from both shipped files. The
  generated engine region declares `Optional` itself and uses it everywhere; the hub
  sources do the same. Both files compile and answer under Python 3.9.6.
- Mass, radius and diameter for every body in `_SCI_BODY` (`hub/sci.py`). "mass of
  earth", "diameter of mars", "radius of the moon", "how big is jupiter", "mass of the
  moon in pounds". Radii are equatorial, so diameters match NASA's figures to the
  digit. Eight new goldens; the first cut stole "schwarzschild radius of the sun" and
  the golden suite caught it.
- `hub/test_artifact_parity.py` now holds the platform's two rules the right way round:
  no future import as code in the shipped file, no PEP 604 union in the engine region,
  and the region is executed with nothing added. The old check had gone vacuous the
  moment a comment mentioned the line.
- Trigger words for the new answers in `deploy/devkit-config.json` and the README, so
  `test_hotword_reach.py` can reach them.
- The engine source and tests ship upstream inside `community/astral/hub/`.
  `hub/build_ability.py` and the parity test work in either layout, and `hub/README.md`
  explains regeneration and the tests. `deploy/build_followup_pr.sh` stages the second
  PR from a clone of the fork; the commit, push and PR are the three commands it prints.
  `hub/` stays out of this public repo as before; only the engine subset ships.
- The normalize docstring lost its last word in the merge ("not what a test."). Restored.

Suites after the change: 155 phrases byte-exact, 155 x 2 artifacts identical to the
hub, hardening clean, hotword reach clean, generator check clean. The `hub/` sources
are now formatted the same way the shipped files always were, because they ship too;
the staged upstream `main.py` is byte-identical to the one here. flake8 with the upstream
flags is clean except E226 inside f-strings, which autopep8 on Python 3.9 cannot see and
which the merged file already carried through their bot.

## Next

Version 2, same name. The direction is in `ROADMAP.md` section 5: a measured cost ranking for every
command so the device knows what it cannot handle, Slate as the exact math tier behind
the router, and the suites lifted into the engine's harnesses.
