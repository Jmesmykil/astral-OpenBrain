__pycache__/
*.pyc
*.pyo
.DS_Store
*.wav
*.log
.venv/
venv/
scratch/
build/
__pycache__/
*.zip

# your own ability names for the packaged config.json (see README), never committed
config.local.json

# internal — kept local, not in the public repo
hub/

# OpenHome's dashboard app, modified for turn routing. Their source, not ours, so it
# is not republished here. protocol.json carries its sha256 and check_installed.py
# verifies the file installed on the device against that, so nothing in the tooling
# needs this file to be present in the repository.
deploy/turn-router/App.tsx
